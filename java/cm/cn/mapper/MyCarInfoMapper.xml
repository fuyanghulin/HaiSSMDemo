<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cm.cn.mapper.MyCarInfoMapper" >
	<delete id="delCarInfoBatch">
		DELETE
		FROM
			hl_carinfo
		WHERE
			id IN 
		<foreach collection="array" item="id" index="index"
	          open="(" close=")" separator=",">
	          #{id}
	    </foreach>
	</delete>
	<select id="selDriver" parameterType="String" resultType="String">
	   SELECT
			driver_name 
	   FROM
			hl_carinfo 
	   WHERE
			car_num = #{plateNo}
	</select>
	<select id="selSafer" parameterType="String" resultType="String">
	   SELECT
			safer 
	   FROM
			hl_carinfo 
	   WHERE
			car_num = #{plateNo}
	</select>
</mapper>